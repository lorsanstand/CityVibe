# 🎉 CityVibe API

> **Платформа для открытия и управления городскими событиями**

[![Python](https://img.shields.io/badge/Python-3.13-3776ab.svg?style=flat-square&logo=python)](https://www.python.org/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.127-009688.svg?style=flat-square&logo=fastapi)](https://fastapi.tiangolo.com/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-Latest-336791.svg?style=flat-square&logo=postgresql)](https://www.postgresql.org/)
[![Docker](https://img.shields.io/badge/Docker-Compose-2496ED.svg?style=flat-square&logo=docker)](https://www.docker.com/)

---

## 📋 Содержание

- [О проекте](#о-проекте)
- [Основные возможности](#основные-возможности)
- [Технический стек](#технический-стек)
- [Предварительные требования](#предварительные-требования)
- [Установка](#установка)
- [Конфигурация](#конфигурация)
- [Запуск приложения](#запуск-приложения)
- [API Документация](#api-документация)
- [Структура проекта](#структура-проекта)
- [Развертывание](#развертывание)
- [Логирование](#логирование)
- [Contributing](#contributing)
- [Лицензия](#лицензия)

---

## 🎯 О проекте

**CityVibe** - это современная RESTful API платформа для управления городскими событиями. Приложение позволяет пользователям находить, создавать и управлять событиями в своем городе, оставлять отзывы, загружать фотографии и сохранять избранные события.

### Целевая аудитория:
- 👥 Обычные пользователи - поиск и участие в событиях
- 🎭 Организаторы событий - создание и управление событиями
- 👨‍💼 Администраторы - управление пользователями и контентом

---

## ✨ Основные возможности

### Для пользователей:
- ✅ Регистрация и аутентификация с email верификацией
- ✅ Поиск событий с фильтрацией по параметрам (название, местоположение, рейтинг и т.д.)
- ✅ Просмотр детальной информации о событиях
- ✅ Добавление и удаление событий из избранного
- ✅ Оставление отзывов и рейтингов для событий
- ✅ Управление личным профилем

### Для организаторов:
- ✅ Создание и редактирование событий
- ✅ Загрузка фотографий событий в облачное хранилище S3
- ✅ Управление информацией о событии (дата, место, емкость и т.д.)
- ✅ Удаление событий и фотографий

### Для администраторов:
- ✅ Полное управление пользователями
- ✅ Отправка push-уведомлений
- ✅ Просмотр и управление всеми событиями в системе

---

## 🛠️ Технический стек

### Backend:
- **FastAPI** (0.127+) - современный веб-фреймворк
- **SQLAlchemy** (2.0+) - ORM для работы с БД
- **AsyncPG** - асинхронный драйвер PostgreSQL
- **Pydantic** - валидация данных
- **JWT** - аутентификация и авторизация

### Базы данных и очереди:
- **PostgreSQL** - основная база данных
- **RabbitMQ** - брокер сообщений
- **Celery** - асинхронная обработка задач

### Облачное хранилище:
- **AWS S3 / MinIO** - хранение фотографий событий

### Дополнительно:
- **Pillow** - обработка изображений
- **Colorlog** - цветное логирование
- **Docker & Docker Compose** - контейнеризация

---

## 📦 Предварительные требования

- **Docker** >= 20.10
- **Docker Compose** >= 2.0
- **Python** >= 3.13 (для локальной разработки)
- **Git**

Или для локальной разработки без Docker:
- PostgreSQL >= 13
- RabbitMQ >= 3.8
- Redis (опционально)
- S3-совместимое хранилище

---

## 🚀 Установка

### Вариант 1: С использованием Docker (Рекомендуется)

```bash
# 1. Клонировать репозиторий
git clone https://github.com/lorsanstand/CityVibe.git
cd CityVibe

# 2. Создать файл переменных окружения
cp .env.example .env

# 3. Заполнить переменные окружения (см. раздел Конфигурация)
nano .env

# 4. Запустить приложение с Docker Compose
docker-compose up -d
```

### Вариант 2: Локальная установка

```bash
# 1. Клонировать репозиторий
git clone https://github.com/lorsanstand/CityVibe.git
cd CityVibe/backend

# 2. Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# 3. Установить зависимости
pip install -r requirements.txt
# или с Poetry:
poetry install

# 4. Создать файл .env в корне backend/
cp .env.example .env

# 5. Запустить миграции БД
alembic upgrade head

# 6. Запустить FastAPI сервер
python -m app.main

# 7. В отдельном терминале запустить Celery worker
celery -A app.celery_app worker -l info
```

---

## ⚙️ Конфигурация

### Основные переменные окружения (.env файл)

```bash
# Режим приложения
MODE=DEV  # DEV, TEST, PROD

# База данных PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=cityvibe_user
DB_PASS=secure_password
DB_NAME=cityvibe_db

# Тестовая база данных
TEST_DB_HOST=localhost
TEST_DB_PORT=5432
TEST_DB_USER=cityvibe_test
TEST_DB_PASS=test_password
TEST_DB_NAME=cityvibe_test

# RabbitMQ
RMQ_HOST=localhost
RMQ_USER=guest
RMQ_PASS=guest
RMQ_PORT=5672

# JWT Токены
SECRET=your-secret-key-here-change-it-in-production
ALGORITHMS=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=30
VERIFY_EMAIL_TOKEN_HOURS=2

# Email (SMTP)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_EMAIL=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# S3 / MinIO
S3_URL=http://localhost:9000  # или https://s3.amazonaws.com
S3_ACCESS_KEY_ID=your_access_key
S3_SECRET_ACCESS_KEY=your_secret_key
S3_BUCKET_NAME=cityvibe-events

# CORS
CORS_ORIGINS=["http://localhost:3000", "http://localhost:8080"]
CORS_METHODS=["GET", "POST", "PUT", "DELETE"]
CORS_HEADERS=["*"]

# Основной URL приложения
URL=http://localhost:8000

# Логирование
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR

# Суперпользователь (для инициализации)
FIRST_SUPERUSER_EMAIL=admin@example.com
FIRST_SUPERUSER_PASS=admin_password

# Хост и порт приложения
HOST=0.0.0.0
PORT=8000
WORKERS=4
```

### Пример для production (.env для prod.env)

```bash
MODE=PROD
# ... остальные переменные с production значениями
```

---

## 🏃 Запуск приложения

### С Docker Compose

```bash
# Запуск всех сервисов
docker-compose up -d

# Просмотр логов
docker-compose logs -f backend

# Остановка приложения
docker-compose down

# Очистка данных (включая БД)
docker-compose down -v
```

### Локально

```bash
# Запуск FastAPI сервера (в одном терминале)
cd backend
python -m app.main

# Запуск Celery worker (в другом терминале)
celery -A app.celery_app worker -l info

# Запуск RabbitMQ (если не в Docker)
rabbitmq-server
```

### Проверка здоровья приложения

```bash
# API должно быть доступно на:
curl http://localhost:8000/

# Swagger UI документация:
http://localhost:8000/docs

# ReDoc документация:
http://localhost:8000/redoc

# Проверка статуса:
curl http://localhost:8000/health  # если добавить endpoint
```

---

## 📚 API Документация

### Интерактивная документация

После запуска приложения откройте в браузере:

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### Основные endpoints

#### Аутентификация
```
POST   /api/auth/register      - Регистрация
POST   /api/auth/login         - Вход в систему
POST   /api/auth/logout        - Выход из системы
POST   /api/auth/refresh       - Обновление токена
POST   /api/auth/verify        - Подтверждение email
```

#### Пользователи
```
GET    /api/users/             - Список всех пользователей (admin)
GET    /api/users/me           - Мой профиль
PUT    /api/users/me           - Обновить мой профиль
DELETE /api/users/me           - Удалить мой профиль
GET    /api/users/{user_id}    - Профиль пользователя (admin)
PUT    /api/users/{user_id}    - Обновить пользователя (admin)
DELETE /api/users/{user_id}    - Удалить пользователя (admin)
```

#### События
```
POST   /api/events/            - Создать событие
GET    /api/events/{event_id}  - Получить событие
POST   /api/events/search      - Поиск событий
PUT    /api/events/{event_id}  - Обновить событие
DELETE /api/events/{event_id}  - Удалить событие
```

#### Фотографии событий
```
POST   /api/events/{event_id}/photo      - Загрузить фото
GET    /api/events/{event_id}/photo      - Получить фото события
DELETE /api/events/{event_id}/photo      - Удалить фото
```

#### Отзывы
```
POST   /api/events/{event_id}/reviews    - Создать отзыв
GET    /api/events/{event_id}/reviews    - Получить отзывы события
PUT    /api/events/{event_id}/reviews    - Обновить свой отзыв
DELETE /api/events/{event_id}/reviews    - Удалить свой отзыв
```

#### Избранное
```
POST   /api/users/me/favorites           - Добавить в избранное
GET    /api/users/me/favorites           - Получить избранные события
DELETE /api/users/me/favorites/{event_id} - Удалить из избранного
```

Более подробная информация доступна в Swagger UI после запуска приложения.

---

## 📁 Структура проекта

```
CityVibe/
├── backend/                          # FastAPI приложение
│   ├── app/
│   │   ├── auth/                    # Аутентификация и авторизация
│   │   │   ├── router.py
│   │   │   ├── service.py
│   │   │   ├── models.py
│   │   │   ├── schemas.py
│   │   │   └── dependencies.py
│   │   │
│   │   ├── users/                   # Управление пользователями
│   │   │   ├── router.py
│   │   │   ├── service.py
│   │   │   ├── models.py
│   │   │   ├── dao.py
│   │   │   └── schemas.py
│   │   │
│   │   ├── events/                  # Управление событиями
│   │   │   ├── router.py
│   │   │   ├── service.py
│   │   │   ├── models.py
│   │   │   ├── dao.py
│   │   │   └── schemas.py
│   │   │
│   │   ├── services/                # Бизнес-логика
│   │   │   ├── email_service.py     # Email отправка
│   │   │   └── S3_service.py        # S3 операции
│   │   │
│   │   ├── utils/                   # Утилиты
│   │   │   ├── email_client.py
│   │   │   └── S3_client.py
│   │   │
│   │   ├── tasks/                   # Celery задачи
│   │   │   ├── email_tasks.py
│   │   │   └── S3_tasks.py
│   │   │
│   │   ├── migration/               # Alembic миграции БД
│   │   ├── templates/               # HTML шаблоны
│   │   │
│   │   ├── base_dao.py              # Базовый DAO класс
│   │   ├── database.py              # Конфигурация БД
│   │   ├── main.py                  # Точка входа приложения
│   │   ├── config.py                # Конфигурация параметров
│   │   ├── log_config.py            # Конфигурация логирования
│   │   ├── exceptions.py            # Пользовательские исключения
│   │   └── constants.py             # Константы приложения
│   │
│   ├── scripts/
│   │   └── prestart.sh              # Скрипт инициализации
│   │
│   ├── tests/                       # Unit тесты
│   │   ├── test_auth.py
│   │   ├── test_user.py
│   │   └── conftest.py
│   │
│   ├── requirements.txt             # Зависимости (pip)
│   ├── pyproject.toml               # Конфигурация Poetry
│   ├── alembic.ini                  # Конфигурация Alembic
│   ├── Dockerfile                   # Docker образ
│   └── poetry.lock                  # Lockfile зависимостей
│
├── docker-compose.yml               # Оркестрация контейнеров
├── .env                             # Переменные окружения (не коммитить!)
├── .env.example                     # Пример переменных окружения
├── .gitignore                       # Git ignore rules
├── .gitlab-ci.yml                   # CI/CD конфигурация (GitLab)
└── README.md                        # Этот файл
```

---

## 🐳 Развертывание

### На сервере с Docker

```bash
# 1. Установить Docker и Docker Compose
sudo apt-get update
sudo apt-get install docker.io docker-compose

# 2. Клонировать репозиторий
git clone https://github.com/lorsanstand/CityVibe.git
cd CityVibe

# 3. Создать production .env файл
cp prod.env .env
# Отредактировать переменные для production

# 4. Создать volumes для данных
docker volume create cityvibe_db
docker volume create cityvibe_rabbitmq

# 5. Запустить приложение
docker-compose up -d

# 6. Проверить логи
docker-compose logs -f backend
```

### SSL сертификаты

Для production используйте Let's Encrypt с nginx reverse proxy:

```bash
# Установить Certbot
sudo apt-get install certbot python3-certbot-nginx

# Получить сертификат
sudo certbot certonly --standalone -d yourdomain.com

# Обновлять автоматически
sudo systemctl enable certbot.timer
```

### Масштабирование

```bash
# Увеличить количество workers
WORKERS=8 docker-compose up -d

# Использовать nginx load balancer
# Конфиг в nginx.conf
docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d
```

---

## 📝 Логирование

Приложение имеет полное структурированное логирование на всех уровнях:

### Уровни логирования:
- 🟢 **INFO** - основные операции (создание, обновление, удаление)
- 🔵 **DEBUG** - детальная информация для отладки
- 🟡 **WARNING** - потенциальные проблемы
- 🔴 **ERROR** - ошибки при выполнении операций

### Просмотр логов

```bash
# В Docker
docker-compose logs -f backend

# Локально
tail -f app.log

# С фильтрацией
docker-compose logs backend | grep "ERROR"
```

### Структурированные логи

Все логи содержат дополнительный контекст:
```json
{
  "timestamp": "2025-12-24T10:30:00",
  "level": "INFO",
  "message": "User registered",
  "user_id": "uuid-string",
  "email": "user@example.com",
  "module": "auth.service"
}
```

Подробнее см. [LOGGING_SUMMARY.md](backend/LOGGING_SUMMARY.md)

---

## 🧪 Тестирование

### Запуск тестов

```bash
# Все тесты
pytest backend/tests/

# С покрытием
pytest backend/tests/ --cov=app

# Конкретный тест
pytest backend/tests/test_auth.py::test_register

# Verbose вывод
pytest backend/tests/ -v
```

### Тестовые данные

```bash
# Создать тестовые данные
python backend/scripts/seed_data.py

# Очистить БД
python backend/scripts/clear_db.py
```

---

## 📊 Мониторинг и Метрики

### Healthcheck endpoints

```bash
# Проверка здоровья БД
curl http://localhost:8000/api/health/db

# Проверка статуса RabbitMQ
curl http://localhost:8000/api/health/rabbitmq

# Общий статус приложения
curl http://localhost:8000/api/health
```

### Логирование запросов

Все HTTP запросы логируются автоматически через middleware:

```
method=GET path=/api/events/123 status=200
method=POST path=/api/auth/login status=401
method=POST path=/api/events status=201
```

---

## 🔒 Безопасность

### Рекомендации для production

1. **Измените все пароли по умолчанию** в переменных окружения
2. **Используйте HTTPS** вместо HTTP
3. **Установите CORS** только для доверенных доменов
4. **Включите rate limiting** для API endpoints
5. **Используйте strong SECRET** для JWT токенов
6. **Регулярно обновляйте зависимости**:
   ```bash
   pip list --outdated
   poetry update
   ```

7. **Настройте WAF** (Web Application Firewall)
8. **Используйте VPN** для доступа к БД
9. **Включите двухфакторную аутентификацию** для админов
10. **Регулярно проверяйте логи** на аномалии

### OWASP Top 10

Приложение защищено от:
- ✅ SQL Injection (SQLAlchemy ORM)
- ✅ XSS атак (Pydantic валидация)
- ✅ CSRF (JWT токены)
- ✅ Brute Force (Passlib хеширование)
- ✅ Broken Authentication (JWT + refresh tokens)

---

## 🐛 Troubleshooting

### Проблема: "Connection refused" для БД

```bash
# Проверить статус контейнера
docker-compose ps

# Перезапустить БД
docker-compose restart database

# Просмотреть логи
docker-compose logs database
```

### Проблема: Celery задачи не выполняются

```bash
# Проверить RabbitMQ
docker-compose exec rabbitmq rabbitmq-diagnostics -q ping

# Перезапустить worker
docker-compose restart celery-worker

# Очистить очередь
docker-compose exec celery-worker celery -A app.celery_app purge
```

### Проблема: S3 загрузка не работает

```bash
# Проверить доступность S3
curl -X HEAD http://localhost:9000/cityvibe-events

# Проверить credentials
aws s3 ls --endpoint-url http://localhost:9000
```

### Проблема: Email не отправляется

```bash
# Проверить SMTP credentials в .env
# Попробовать отправить тестовое письмо
python -c "from app.services.email_service import EmailService; EmailService.send_verify_email('test@example.com', 'Test', 'http://localhost:8000/verify?token=test')"
```

---

## 📞 Контакты и поддержка

- **Email**: stasstrochewskij@gmail.com
- **GitHub Issues**: https://github.com/lorsanstand/CityVibe/issues
- **Документация API**: http://localhost:8000/docs

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробнее см. [LICENSE](LICENSE)

---

## 🙏 Благодарности

- FastAPI сообщество за отличный фреймворк
- SQLAlchemy за мощную ORM
- Docker за контейнеризацию
- Всем контрибьюторам проекта

---

## 🔄 Версионирование

Проект использует семантическое версионирование (SemVer):
- **MAJOR** - несовместимые изменения API
- **MINOR** - новая функциональность (обратно совместимо)
- **PATCH** - исправления ошибок

Текущая версия: **0.1.0** (alpha)

---

## 📈 Дорожная карта

### v0.2.0 (Планируется)
- [ ] Интеграция с Google Maps API
- [ ] Real-time уведомления через WebSocket
- [ ] Рекомендации событий на основе ML
- [ ] Мобильное приложение (React Native)

### v0.3.0 (Планируется)
- [ ] Социальные функции (подписки, комментарии)
- [ ] Интеграция с платежами (Stripe/Yandex.Kassa)
- [ ] Календарь событий
- [ ] Экспорт в ICS формат

---

**Спасибо за использование CityVibe! 🎉**

Made with ❤️ by Stanislav Strochesky
