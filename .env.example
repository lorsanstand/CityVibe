# CityVibe Backend - Environment Variables Example
# Copy this file to .env and fill in your actual values

# ============================================
# ENVIRONMENT SETTINGS
# ============================================

# Application mode: DEV, TEST, or PROD
MODE=DEV

# ============================================
# DATABASE SETTINGS (PostgreSQL)
# ============================================

# Production database
DB_HOST=localhost
DB_PORT=5432
DB_USER=cityvibe_user
DB_PASS=your_secure_password_here
DB_NAME=cityvibe_db

# Test database (for running tests)
TEST_DB_HOST=localhost
TEST_DB_PORT=5432
TEST_DB_USER=cityvibe_test
TEST_DB_PASS=test_password
TEST_DB_NAME=cityvibe_test_db

# ============================================
# RABBITMQ SETTINGS
# ============================================

# RabbitMQ message broker for async tasks
RMQ_HOST=localhost
RMQ_USER=guest
RMQ_PASS=guest
RMQ_PORT=5672

# ============================================
# JWT AUTHENTICATION SETTINGS
# ============================================

# Secret key for signing JWT tokens (generate a strong random key!)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET=your-super-secret-key-change-in-production

# JWT algorithm (recommended: HS256)
ALGORITHMS=HS256

# Token expiration times
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=30
VERIFY_EMAIL_TOKEN_HOURS=2

# ============================================
# EMAIL SETTINGS (SMTP)
# ============================================

# Gmail example (enable "Less secure app access" or use App Password)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_EMAIL=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Alternative: other SMTP servers
# Yandex Mail:
# SMTP_SERVER=smtp.yandex.com
# SMTP_PORT=587
# SendGrid:
# SMTP_SERVER=smtp.sendgrid.net
# SMTP_PORT=587

# ============================================
# AWS S3 / MinIO SETTINGS
# ============================================

# For MinIO (local development):
S3_URL=http://localhost:9000

# For AWS S3:
# S3_URL=https://s3.amazonaws.com
# S3_URL=https://s3.eu-west-1.amazonaws.com

S3_ACCESS_KEY_ID=your_access_key_id
S3_SECRET_ACCESS_KEY=your_secret_access_key
S3_BUCKET_NAME=cityvibe-events

# ============================================
# CORS SETTINGS
# ============================================

# Allowed origins for CORS (JSON array)
CORS_ORIGINS=["http://localhost:3000","http://localhost:8080","http://127.0.0.1:3000"]

# Allowed HTTP methods
CORS_METHODS=["GET","POST","PUT","DELETE","OPTIONS"]

# Allowed headers
CORS_HEADERS=["*"]

# ============================================
# APPLICATION SETTINGS
# ============================================

# Base URL of your application (used for email links)
URL=http://localhost:8000

# Server host and port
HOST=0.0.0.0
PORT=8000

# Number of Uvicorn workers (multiply by 2 + 1 for CPU cores)
WORKERS=4

# ============================================
# LOGGING SETTINGS
# ============================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================
# INITIAL SUPERUSER SETTINGS
# ============================================

# These are used to create the initial admin user when the app starts
FIRST_SUPERUSER_EMAIL=admin@example.com
FIRST_SUPERUSER_PASS=admin_secure_password_123

# ============================================
# NOTES
# ============================================

# 1. NEVER commit this file to git with real credentials
# 2. Use .env.example as template for new developers
# 3. Generate strong passwords for production:
#    python -c "import secrets; print(secrets.token_urlsafe(32))"
# 4. For production, use environment variables from your deployment platform
# 5. Consider using tools like:
#    - AWS Secrets Manager
#    - HashiCorp Vault
#    - Azure Key Vault
#    - GitLab/GitHub Secrets
